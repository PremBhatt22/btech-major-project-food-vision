import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["upload.wikimedia.org", "images.unsplash.com", "images.pexels.com", "en.wikipedia.org", "www.freepik.com"],
  },
};

export default nextConfig;
